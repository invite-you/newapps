# 프록시 설정 예시
# 이 파일을 복사하여 .env 파일을 만들고 실제 프록시 주소로 수정하세요
# cp .env.example .env

# HTTP 프록시 (선택사항)
# 예: HTTP_PROXY=http://proxy.example.com:8080
# HTTP_PROXY=

# HTTPS 프록시 (선택사항)
# 예: HTTPS_PROXY=https://proxy.example.com:8080
# HTTPS_PROXY=

# 인증이 필요한 프록시 예시
# HTTP_PROXY=http://username:password@proxy.example.com:8080
# HTTPS_PROXY=https://username:password@proxy.example.com:8080

# 프록시 미설정 시 일반 통신 사용
